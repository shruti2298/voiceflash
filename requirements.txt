# web / api
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
pydantic-settings==2.*

# db
sqlalchemy==2.*
psycopg2-binary==2.9.*

# cache — Redis so active-session/leaderboard state is shared across worker
# processes/instances, not just a single process's memory
redis==5.*

# voice (Pipecat + services). Pinned to the exact version verified against this repo.
pipecat-ai[deepgram,groq,silero,webrtc]==0.0.108

# dev/test
pytest==8.*
httpx==0.27.*        # FastAPI TestClient
python-dotenv==1.*
fakeredis==2.*       # in-memory Redis stand-in so tests never need a real Redis
